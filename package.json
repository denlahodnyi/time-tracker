{
  "name": "time-tracker",
  "private": true,
  "author": "Den Lahodnyi",
  "version": "1.0.0",
  "description": "",
  "keywords": [],
  "license": "",
  "engines": {
    "node": ">=18",
    "pnpm": ">=9"
  },
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "api:dev": "pnpm -F backend dev",
    "api:build": "pnpm -F backend build",
    "api:start": "pnpm -F backend start",
    "api:test:unit": "pnpm -F backend test:unit",
    "api:test:int": "pnpm -F backend test:int",
    "api:seed:dev": "pnpm -F backend seed:dev",
    "web:dev": "pnpm -F web dev",
    "web:build": "pnpm -F web build",
    "web:start": "pnpm -F web start",
    "prisma": "pnpm -F @libs/prisma prisma",
    "prisma:build": "pnpm -F @libs/prisma build",
    "prisma:generate": "pnpm -F @libs/prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
